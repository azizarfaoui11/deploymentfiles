pipeline {
    agent any
    stages {
        stage('Test Webhook') {
            steps {
                script {
                    echo 'Vérification du Webhook...'

            }
        }
    }
}
}
